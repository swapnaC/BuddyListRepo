{
  "name": "angular-apple-challenge",
  "private": true,
  "version": "0.0.0",
  "description": "A Buddies list for Interview",
  "repository": "https://github.com/swapnaC/BuddyListRepo",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8080 -c-1"
  }
}
